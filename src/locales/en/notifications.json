{
  "EXCEPTION": "{{- name }} has errored {{ build }}",
  "FAILURE": "{{- name }} has failed {{ build }}",
  "OPT_ALL": "Display on all builds",
  "OPT_CHANGE": "Display on status change",
  "OPT_FAIL": "Display on unsuccessful build",
  "SUCCESS": "{{- name }} is now passing {{ build }}",
  "UNKNOWN": "{{- name }} is in an unknown state {{ build }}"
}
